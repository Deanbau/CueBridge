# OSC Switcher Bridge — requirements
# Python 3.11+ recommended

nicegui>=2.0.0          # Web UI framework
python-osc>=1.8.3       # OSC server/client
aiohttp>=3.9.0          # Async HTTP (Barco REST) + WebSocket (PixelHue)
zeroconf>=0.131.0       # mDNS/Bonjour service advertisement
pyatem>=0.9.0           # Blackmagic ATEM switcher protocol
